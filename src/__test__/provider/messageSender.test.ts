import { IMessageSender, MessageSender } from "../../provider/messageSender";

describe("Filter function", () => {
  test("it should filter by a search term (link)", () => {

        //const myMock = MessageSender as jest.MockedClass<typeof MessageSender>;
        //const q = myMock.mock.instances[0];
        //const myMock = mocked(QueueServiceClient, true);
        //const mSender = new MessageSender(q, "");
  });
});
